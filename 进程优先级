查看进程优先级命令：top

PR：进程的实际调度优先级，内核根据 PR 值决定调度哪个进程，PR 越低优先级越高。
普通进程的 PR = 20 + nice 值 （20 ～ 39）
实时进程的 PR 范围为 0~99，并不受 nice 影响，而由调度策略（如 SCHED_RR、SCHED_FIFO）决定 （0 ~ 99）

NI：nice值，影响调度器分配 CPU 的倾向（时间片jeffies 的分配），但并不是最终决定优先级的唯一因素
一般考虑在用户层面对进程调度优先级的建议值

%ni（或 %nice、ni 列）是 0.0%，说明当前系统上没有被提升 nice 值（nice>0，即优先级被降低）的进程在消耗 CPU。
换句话说，所有在消耗 CPU 的进程，其 nice 值都是 0 或者更小（即默认或优先级被提高）的。

top cpu参数含义：
us	用户空间进程消耗的 CPU
sy	内核空间进程消耗的 CPU
ni	nice 值 > 0 进程的 CPU
id	空闲
wa	IO 等待
hi	硬件中断
si	软件中断
st	被虚拟机偷取的 CPU
